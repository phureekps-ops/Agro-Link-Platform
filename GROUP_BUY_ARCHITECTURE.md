# AgroLink Group Buy (รวมออเดอร์ประมูลร่วมของสหกรณ์) — สถาปัตยกรรม

**สถานะเอกสาร:** ฉบับที่ 2 — แก้ไข 2026-08-25: สร้างจริงแล้ว (DB migration `backend/db/grant_group_buy.sql`, backend `src/routes/groupbuy.js` + ส่วนต่อขยายใน `src/routes/admin.js`, frontend ใน `frontend/coop/dashboard.html`/`js/dashboard.js` และ `frontend/admin/group-buys.html`/`js/group-buys.js`) ตามการตัดสินใจ 3 ข้อที่ยืนยันกับผู้ใช้แล้ว (ดูข้อ 6 ที่อัปเดต): (1) สหกรณ์เปิดรอบเองได้อิสระ ไม่ต้องขออนุมัติก่อน (2) ทีมงาน AgroLink เป็นผู้เลือก/อนุมัติ "สหกรณ์หัวขบวน" เป็นรายรอบ ไม่ใช้กติกาอัตโนมัติ (3) ส่งของจุดเดียวที่สหกรณ์หัวขบวน สหกรณ์อื่นมารับต่อเองนอกระบบในรอบแรก ฉบับที่ 1 (ข้อเสนอก่อนตัดสินใจ) คือเนื้อหาด้านล่างนี้ที่ปรับปรุงแล้ว
**ผู้ร้องขอ:** ต่อยอดจากฟีเจอร์ "ประมูลซื้อแม่ปุ๋ยและปัจจัยการผลิตปริมาณมาก" ในพอร์ทัลสหกรณ์ — ปัจจุบันแต่ละสหกรณ์เปิดประมูล (e-Auction) แยกกันเอง เอกสารนี้เสนอวิธีให้ **หลายสหกรณ์รวมปริมาณความต้องการเข้าด้วยกันก่อน แล้วค่อยเปิดประมูลเป็นก้อนเดียว** เพื่อให้ได้อำนาจต่อรองราคาที่ดีกว่า

---

## 1. หลักการออกแบบ

1. **ต่อยอดของเดิม ไม่สร้างระบบประมูลใหม่.** ระบบ RFQ (`procurement.rfq`) และ e-Auction (`procurement.auction`/`auction_bid`) ที่มีอยู่แล้วทำหน้าที่ "แข่งราคาแบบปิด (sealed-bid) แล้วตัดสินผู้ชนะอัตโนมัติ" ได้ดีอยู่แล้ว — สิ่งที่ยังไม่มีคือ **ขั้นตอนก่อนหน้านั้น** คือการรวบรวมความต้องการจากหลายสหกรณ์ให้เป็นปริมาณเดียวก่อนแปลงเป็น RFQ/Auction ใบเดียว เอกสารนี้จึงเสนอเพิ่มเฉพาะ "ชั้นรวบรวมออเดอร์" ไว้หน้า RFQ/Auction เดิม ไม่แตะโค้ด/ตารางประมูลเดิมเลย
2. **Contract/PO/GRN/Invoice/Payment ยังเป็นความสัมพันธ์ผู้ซื้อ 1 ราย ↔ ผู้ขาย 1 ราย เหมือนเดิม.** schema ปัจจุบันของ `contract.contract` ผูกกับผู้ซื้อหนึ่งองค์กรเสมอ การให้ "กลุ่มสหกรณ์" เป็นผู้ซื้อร่วมโดยตรงจะต้องแก้ schema หลายจุดและกระทบของเดิมที่ทำงานอยู่แล้ว จึงเสนอให้ใช้วิธี **"สหกรณ์หัวขบวน" (Lead Cooperative)** เป็นผู้เซ็นสัญญา/รับของ/จ่ายเงินจริงในนามกลุ่มแทน แล้วค่อย "แบ่งต้นทุนคืน" ให้สหกรณ์ที่ร่วมรอบภายหลัง — สอดคล้องกับกลไก **Revenue Sharing** ที่มีอยู่แล้วในระบบ (ตอนนี้ใช้แบ่ง "รายได้" จากสหกรณ์คืนสมาชิกเกษตรกร) เพียงแต่ทิศทางกลับด้าน คือแบ่ง "ต้นทุน" จากสหกรณ์หัวขบวนคืนไปยังสหกรณ์ผู้ร่วมรอบ ใช้ตรรกะการคำนวณสัดส่วนแบบเดียวกัน
3. **Ledger เป็นจุดเดียวที่อนุญาตให้เงินเคลื่อนไหว** เหมือนกฎเดิมของทั้งระบบ — การเรียกเก็บเงินคืนจากสหกรณ์ผู้ร่วมรอบต้องผ่าน `ledger.transfer_funds()` เท่านั้น ไม่มีการปรับยอดตรงในตาราง
4. **"Manual today, real integration later"** — ประเด็นที่ยังไม่มีคำตอบชัดเจนเชิงธุรกิจ (ดูข้อ 6) จะระบุไว้ตรง ๆ ว่ายังต้องมนุษย์ตัดสินใจ/ประสานงานนอกระบบในรอบแรก ไม่เดาแทน

---

## 2. ภาพรวม Flow

```mermaid
flowchart TD
    A[สหกรณ์ A เปิดรอบ Group Buy\nระบุสินค้า + ปริมาณขั้นต่ำรวม + วันปิดรับ] --> B[ประกาศรอบให้สหกรณ์อื่นเห็น]
    B --> C[สหกรณ์ B, C, D... เข้าร่วม\nแจ้งปริมาณที่ต้องการ]
    C --> D{ถึงวันปิดรับ\nหรือยอดรวมถึงขั้นต่ำ}
    D -->|ยัง| C
    D -->|ปิดรอบ| E[ระบบเลือก \"สหกรณ์หัวขบวน\"\nและสร้าง RFQ+Auction รวม\nด้วยปริมาณ = ผลรวมทุกราย]
    E --> F[ซัพพลายเออร์แข่งราคา\ne-Auction เดิมไม่แก้ไข]
    F --> G[ปิดประมูล → สร้าง Contract/PO\nระหว่างสหกรณ์หัวขบวน กับ ผู้ชนะ]
    G --> H[รับของ/ตรวจรับ GRN\nออก Invoice จ่ายเงินผ่าน Ledger\nเหมือนดีลปกติทุกประการ]
    H --> I[ระบบคำนวณสัดส่วนต้นทุน\nตามปริมาณที่แต่ละสหกรณ์แจ้งไว้]
    I --> J[แต่ละสหกรณ์ผู้ร่วมรอบ\nโอนส่วนแบ่งต้นทุนให้สหกรณ์หัวขบวน\nผ่าน ledger.transfer_funds]
    J --> K[ปิดรอบ Group Buy\nพร้อมรายงานสรุปให้ทุกฝ่าย]
```

**หมายเหตุ:** ขั้น F–H คือเส้นทาง RFQ→Auction→Contract→PO→GRN→Invoice→Payment **เดิมของระบบทุกจุด ไม่มีการแก้ไขโค้ดส่วนนี้เลย** — group buy เป็นเพียง "ชั้นห่อ" ที่คำนวณปริมาณรวมและระบุสหกรณ์หัวขบวนให้ก่อนเข้าสู่เส้นทางเดิม

---

## 3. Schema ใหม่ที่เสนอ

ตารางใหม่ 2 ตาราง + ฟังก์ชัน 1 ตัว (ตั้งชื่อ schema `procurement.*` เดิมให้สอดคล้องกับ RFQ/Auction ที่อยู่ schema เดียวกัน):

### 3.1 `procurement.group_buy`
รอบรวมออเดอร์หนึ่งรอบ — ยังไม่ผูกกับ RFQ จนกว่าจะปิดรอบสำเร็จ

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `group_buy_id` | uuid PK | |
| `initiator_org_id` | uuid FK → `identity.organization` | สหกรณ์ที่เปิดรอบ (ไม่จำเป็นต้องเป็นหัวขบวนตอนปิดรอบเสมอไป — ดูข้อ 6.3) |
| `category` | text | ใช้ domain เดียวกับ `rfq.category` (`input_product` เป็นหลักสำหรับ use case นี้) |
| `product_description` | text | เช่น "ปุ๋ยยูเรีย 46-0-0" |
| `target_unit` | text | หน่วยนับ เช่น "กก." "ตัน" |
| `min_total_qty` | numeric | ปริมาณรวมขั้นต่ำก่อนจะแปลงเป็นประมูลได้ (nullable — ถ้าไม่ตั้งแปลว่าปิดตามเวลาอย่างเดียว) |
| `opens_at` / `closes_at` | timestamptz | ช่วงเวลารับสมัครเข้าร่วม |
| `status` | text | `collecting` → `converted` (สร้าง RFQ/Auction แล้ว) / `cancelled` (ไม่ถึงขั้นต่ำ) |
| `lead_org_id` | uuid FK, nullable | กำหนดค่าตอน "ปิดรอบ" เท่านั้น (ดูข้อ 6.3 ว่าใครเป็นคนเลือก) |
| `converted_rfq_id` | uuid FK → `procurement.rfq`, nullable | ลิงก์กลับไป RFQ/Auction ที่สร้างจากรอบนี้ |
| `created_at` | timestamptz | |

### 3.2 `procurement.group_buy_participant`
แต่ละแถวคือสหกรณ์หนึ่งรายที่เข้าร่วมหนึ่งรอบ

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `participant_id` | uuid PK | |
| `group_buy_id` | uuid FK | |
| `org_id` | uuid FK → `identity.organization` | ต้องเป็น org ที่ผ่าน KYB Verified เท่านั้น (เช็คแบบเดียวกับทุกจุดในระบบ) |
| `requested_qty` | numeric | ปริมาณที่แจ้งไว้ ณ ตอนเข้าร่วม |
| `status` | text | `joined` / `withdrawn` |
| `joined_at` / `withdrawn_at` | timestamptz | |
| UNIQUE (`group_buy_id`, `org_id`) | | หนึ่งสหกรณ์เข้าร่วมได้ 1 แถวต่อรอบ (แก้ไขปริมาณได้ด้วย UPDATE ไม่ใช่ INSERT ซ้ำ) |

### 3.3 `procurement.group_buy_settlement`
บันทึกผลการแบ่งต้นทุนคืน — เป็น audit trail แยกจากการโอนเงินจริงใน ledger (เหมือนที่ Revenue Sharing มีตารางบันทึกแยกจาก `ledger.journal_*` อยู่แล้ว)

| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `settlement_id` | uuid PK | |
| `group_buy_id` | uuid FK | |
| `participant_org_id` | uuid FK | |
| `share_pct` | numeric | `requested_qty ÷ ยอดรวมทั้งรอบ` ณ ตอนปิดรับ |
| `allocated_amount` | numeric | `share_pct × invoice.total_amount` ของ invoice จริงที่หัวขบวนได้รับจากซัพพลายเออร์ |
| `transfer_id` | uuid FK → `ledger.journal_*` (ชื่อจริงตามที่ `ledger.transfer_funds()` คืนค่า) | อ้างอิงธุรกรรมจริงที่โอนแล้ว |
| `settled_at` | timestamptz | |

**ฟังก์ชันใหม่ 1 ตัว:** `procurement.settle_group_buy_costs(p_group_buy_id)` — อ่าน invoice ของ PO ที่เกิดจาก `converted_rfq_id`, คำนวณสัดส่วนจาก `group_buy_participant.requested_qty` (เฉพาะแถว `status='joined'`), เรียก `ledger.transfer_funds()` ให้แต่ละสหกรณ์โอนส่วนแบ่งไปยังสหกรณ์หัวขบวน, แล้ว INSERT ผลลัพธ์ลง `group_buy_settlement` — โครงสร้างฟังก์ชันนี้แทบจะ copy รูปแบบมาจากฟังก์ชัน revenue-share ที่มีอยู่แล้วในโมดูล B2B Commerce Engine (แค่กลับทิศทางเงิน)

---

## 4. API ที่เสนอ (ไฟล์ใหม่ `backend/src/routes/groupbuy.js` mount ที่ `/procurement/group-buys`)

| Endpoint | ใครเรียกได้ | ทำอะไร |
|---|---|---|
| `POST /procurement/group-buys` | สหกรณ์ที่ผ่าน KYB | เปิดรอบใหม่ (`category`, `product_description`, `target_unit`, `min_total_qty?`, `closes_at`) |
| `GET /procurement/group-buys?status=collecting` | ทุกสหกรณ์ | เรียกดูรอบที่เปิดรับอยู่ พร้อมยอดรวมปัจจุบัน |
| `GET /procurement/group-buys/:id` | ทุกสหกรณ์ | รายละเอียด + รายชื่อผู้ร่วม + progress bar (ยอดรวม/ขั้นต่ำ) |
| `POST /procurement/group-buys/:id/join` | สหกรณ์ | แจ้งปริมาณเข้าร่วม (`requested_qty`) — ต้องยัง `collecting` |
| `PATCH /procurement/group-buys/:id/join` | สหกรณ์ผู้ร่วม | แก้ไขปริมาณของตัวเองก่อนปิดรอบ |
| `POST /procurement/group-buys/:id/withdraw` | สหกรณ์ผู้ร่วม | ถอนตัวก่อนปิดรอบ |
| `POST /procurement/group-buys/:id/convert` | ตามข้อ 6.1/6.3 (ผู้ริเริ่มรอบ หรือ Admin) | ปิดรับ + เลือกหัวขบวน + สร้าง `rfq`+`auction` จริงด้วยปริมาณรวม (เรียกโค้ดเดิม ไม่เขียนใหม่) |
| `POST /procurement/group-buys/:id/settle` | หัวขบวน หลัง Invoice จ่ายแล้ว | เรียก `settle_group_buy_costs()` |

---

## 5. UI ที่เสนอ (เพิ่มใน `frontend/coop/dashboard.html` — แท็บใหม่ "รวมออเดอร์ประมูลร่วม")

- **เปิดรอบใหม่:** ฟอร์มเดียวกับการเปิด RFQ ปัจจุบัน + เพิ่มช่อง "ปริมาณขั้นต่ำรวม" และ "วันปิดรับสมัคร"
- **รอบที่เปิดอยู่ (ของทุกสหกรณ์):** การ์ดแสดง progress bar (`ยอดรวมตอนนี้ / ขั้นต่ำที่ตั้งไว้`) + ปุ่ม "เข้าร่วม" พร้อมช่องกรอกปริมาณ
- **รอบของฉัน:** รอบที่เปิดเองหรือเข้าร่วม พร้อมสถานะ และหลังปิดรอบแล้วแสดงผลประมูล + ส่วนแบ่งต้นทุนที่ต้องจ่าย/ได้รับคืน (ถ้าเป็นหัวขบวน)

---

## 6. ประเด็นที่ตัดสินใจแล้ว (ยืนยันกับผู้ใช้ 2026-08-25)

1. **ใครเปิดรอบได้:** ✅ สหกรณ์ทุกแห่งที่ผ่าน KYB เปิดรอบเองได้อิสระ ไม่ต้องขออนุมัติก่อน (`POST /procurement/group-buys` ใน `groupbuy.js`)
2. **เกณฑ์ปิดรอบ:** ปิดเมื่อถึง `closes_at` ที่ตั้งไว้ — ตรวจสอบ lazy ตอนเรียก join/convert เหมือนกลไก `closes_at` ของ e-Auction เดิม ไม่ใช้ cron แยก ทีมงาน AgroLink เห็น badge "พร้อมแปลงเป็นประมูลแล้ว" ในหน้าแอดมินเมื่อถึงเวลาหรือยอดรวมถึง `min_total_qty` แล้ว (ตัวเลือกไหนถึงก่อนก็แปลงได้)
3. **ใครเป็น "สหกรณ์หัวขบวน":** ✅ ทีมงาน AgroLink เลือก/อนุมัติเป็นรายรอบตอนกด "แปลงเป็นประมูล" ในหน้า `frontend/admin/group-buys.html` — เลือกได้เฉพาะสหกรณ์ที่มีสถานะ `joined` ในรอบนั้นเท่านั้น (บังคับด้วย `POST /admin/group-buys/:id/convert`)
4. **การส่งของจริง:** ✅ ส่งจุดเดียวที่สหกรณ์หัวขบวน สหกรณ์อื่นมารับต่อเองนอกระบบในรอบแรก (ไม่แตะ Logistics module)
5. **ความเสี่ยงผิดนัด:** ยังไม่มีมัดจำ/บทลงโทษในรอบแรกตามที่ตัดสินใจไว้ — ตกลงกันเองนอกระบบไปก่อน (ดูข้อ 7 ตาราง MVP ว่าเป็นรอบถัดไป)

---

## 7. ขอบเขตที่สร้างจริงในรอบแรก (MVP) เทียบกับรอบถัดไป

| งาน | รอบแรก (MVP) | รอบถัดไป |
|---|---|---|
| เปิดรอบ/เข้าร่วม/ถอนตัว/ดู progress | ✅ สร้างแล้ว | |
| แปลงเป็น RFQ+Auction เมื่อปิดรอบ | ✅ สร้างแล้ว (Admin กดแปลง ไม่ใช่อัตโนมัติเต็มรูปแบบ) | ปิดรอบอัตโนมัติเมื่อถึงเวลา/ยอดขั้นต่ำโดยไม่ต้องรอ Admin กด |
| แบ่งต้นทุนคืนหัวขบวนผ่าน Ledger | ✅ สร้างแล้ว (หัวขบวนกดเองหลัง Invoice จ่ายแล้ว) | |
| เลือกหัวขบวน | ✅ สร้างแล้ว — ทีมงาน AgroLink เลือกเป็นรายรอบ (ตามข้อ 6.3) | ให้สหกรณ์โหวตกันเองในรอบถัดไป ถ้าต้องการ |
| รองรับส่งของหลายจุด | ❌ (ส่งจุดเดียว นอกระบบกระจายเอง — ตามข้อ 6.4) | เชื่อม Logistics module |
| มัดจำ/บทลงโทษผิดนัด | ❌ (ตกลงกันเองนอกระบบ — ตามข้อ 6.5) | ผูกกับ Credit Score ที่ระบบมีแผนไว้แล้ว (ดู B2B_COMMERCE_ENGINE_ARCHITECTURE.md ข้อ 7 roadmap) |
| Admin อนุมัติก่อนเปิดรอบ | ❌ (เปิดได้อิสระ — ตามข้อ 6.1) | ถ้าต้องคุมคุณภาพเพิ่มในอนาคต |

---

## 8. ไฟล์ที่แตะจริง (สร้างเสร็จ 2026-08-25)

| ไฟล์ | การเปลี่ยนแปลง |
|---|---|
| `backend/db/grant_group_buy.sql` (ใหม่) | สร้าง 4 ตาราง (`group_buy`, `group_buy_participant`, `group_buy_settlement_plan`, `group_buy_settlement_line`) + ฟังก์ชัน `create_group_buy_settlement_plan()` และ `distribute_group_buy_settlement()` + GRANT |
| `backend/src/routes/groupbuy.js` (ใหม่) | Endpoint ฝั่งสหกรณ์: เปิดรอบ/เข้าร่วม/ถอนตัว/ดูรายละเอียด/ดูของฉัน/ดูผลแบ่งต้นทุน/กดแบ่งต้นทุนคืน |
| `backend/src/routes/admin.js` | เพิ่ม `GET /admin/group-buys`, `GET /admin/group-buys/:id`, `POST /admin/group-buys/:id/convert` (platform-ops เท่านั้น) |
| `backend/src/server.js` | เพิ่ม `app.use('/procurement', groupBuyRouter)` (ใช้ prefix ร่วมกับ `procurementRouter` เดิม) |
| `frontend/coop/dashboard.html` + `frontend/coop/js/dashboard.js` | เพิ่มส่วน "🤝 รวมออเดอร์ประมูลร่วม (Group Buy)" ตามข้อ 5 |
| `frontend/admin/group-buys.html` + `frontend/admin/js/group-buys.js` (ใหม่) | หน้าแอดมินเลือกสหกรณ์หัวขบวน + กดแปลงเป็นประมูล |
| `frontend/admin/dashboard.html` | เพิ่มลิงก์เมนู "รวมออเดอร์ประมูลร่วม" |
| `coop-portal.html` | ปรับคำอธิบายหัวข้อ "ประมูลซื้อแม่ปุ๋ย" ให้กล่าวถึงการรวมออเดอร์นี้แล้ว |
